{
  "name": "ai-sessions",
  "displayName": "AI Sessions",
  "description": "One panel for every AI coding session: Claude, Codex and friends, with forks.",
  "version": "0.0.1",
  "private": true,
  "publisher": "gregorg",
  "engines": {
    "vscode": "^1.137.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "aiSessions",
          "title": "AI Sessions",
          "icon": "media/activity.svg"
        }
      ]
    },
    "views": {
      "aiSessions": [
        {
          "type": "webview",
          "id": "aiSessions.sidebar",
          "name": "Sessions"
        }
      ]
    },
    "commands": [
      {
        "command": "aiSessions.newSession",
        "title": "New Session",
        "category": "AI Sessions",
        "icon": "$(add)"
      },
      {
        "command": "aiSessions.openAsTab",
        "title": "Open as Editor Tab",
        "category": "AI Sessions",
        "icon": "$(open-preview)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "aiSessions.newSession",
          "when": "view == aiSessions.sidebar",
          "group": "navigation@1"
        },
        {
          "command": "aiSessions.openAsTab",
          "when": "view == aiSessions.sidebar",
          "group": "navigation@2"
        }
      ]
    },
    "configuration": {
      "title": "AI Sessions",
      "properties": {
        "aiSessions.backend": {
          "type": "string",
          "enum": [
            "real",
            "mock"
          ],
          "default": "real",
          "enumDescriptions": [
            "Run sessions through the installed Claude Code and Codex CLIs.",
            "Fake providers with seeded sessions, for working on the UI."
          ],
          "description": "Where sessions run."
        },
        "aiSessions.claudePath": {
          "type": "string",
          "default": "",
          "description": "Path to the claude executable. Empty: look on PATH and the usual install locations."
        },
        "aiSessions.keepAwake": {
          "type": "boolean",
          "default": true,
          "description": "Keep the computer from idle-sleeping while an agent is working (macOS). The display can still sleep."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p src/webview/tsconfig.json",
    "vscode:prepublish": "npm run build -- --production"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@types/vscode": "^1.137.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.282"
  }
}
